# Copy this file to .env and fill in your values.
# NEVER commit .env to git.

# API base URL (no trailing slash)
API_URL=https://api.example.com

# Add any other non-secret runtime config here.
# ⚠️  DO NOT put secret keys here — they will be bundled into the app binary.
#     Use a backend proxy for any secrets that must stay private.
